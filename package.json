{
  "name": "nexella-trigger-server",
  "version": "1.0.0",
  "description": "Nexella Trigger Server with Calendly integration",
  "main": "trigger-server.js",
  "scripts": {
    "start": "node trigger-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=14"
  }
}